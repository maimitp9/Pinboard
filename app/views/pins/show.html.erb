<%= image_tag @pin.image.url(:medium) %>
<h2><%= @pin.title %></h2>
<p><%= @pin.discription %></p>
<p>Submited by: <%= @pin.user.email %></p>
<%= link_to 'Edit', edit_pin_path %> | 
<%= link_to 'Delete', @pin, method: :delete, data: { confirm: 'Are you sure to delete?' } %> |
<%= link_to 'Back', pins_path %>